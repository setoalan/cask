<% title 'Listing Stars' %>
<h1>Listing Stars</h1>

<table class='table table-striped table-hover'>
  <thead>
    <tr>
      <th>User</th>
      <th>Brewery</th>
    </tr>
  </thead>

  <tbody>
    <% @stars.each do |star| %>
      <tr>
        <td><%= star.user.name %></td>
        <td><%= star.brewery.brewery_name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
